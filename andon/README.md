# Andon — los gates deterministas

> El cordón que para la línea. En Toyota, cualquier operario que ve un defecto jala el Andon y detiene la producción. Aquí, el Andon es el conjunto de gates que **atrapan el error en la fuente, fuera del LLM** — no le piden al agente que se porte bien; hacen el error imposible o lo bloquean.

## La ley: una sola fuente

Toda regla de gate vive en **un solo lugar** — el `blast-radius.json` (el "radio de explosión": qué área, al tocarse, obliga a tocar qué doc dueño). El hook, el verificador local y el CI **leen esa ley**; la prosa solo la explica. Cambiar la regla en un lugar la cambia en todos.

> **Dónde vive:** en un proyecto instalado, la ley es `.jidoka/blast-radius.json`. Jidoka corre su propio Andon (dogfooding), así que en **este** repo el motor vive en [`../tools/`](../tools/) y la ley es [`../tools/blast-radius.json`](../tools/blast-radius.json).

## Autoridad creciente (el modelo de amenaza)

No todos los gates son muros. Se ordenan por autoridad, y solo el último es infranqueable:

```
Stop hooks (avisan / frenan el cierre)   →  saltables
  pre-push  (UX local)                   →  saltable con --no-verify
    check `andon` en CI + branch protection  →  EL MURO REAL (server-side)
```

Se asume que **todo aviso local será bypaseado**. Por eso el muro real es el check `andon` corriendo en CI con *required checks* activados — lo único que `--no-verify` no salta.

## deny vs ask — heredado de la aviación

Este eje no lo inventamos: sale de cómo la aviación resolvió la automatización peligrosa (ver [`../doctrina/03-aviacion.md`](../doctrina/03-aviacion.md)).

- **`deny`** (bloqueo duro, **estilo Airbus**: límites que no puedes cruzar) para lo **irreversible y peligroso**.
- **`ask`** con override (**estilo Boeing**: avisa y frena, pero puedes anular) para lo que requiere **juicio humano**.

Se mezclan por tipo de acción; no se elige una sola filosofía. Y cuando la situación se degrada, la regla es **`click-it-down`** ("Children of the Magenta"): baja el nivel de automatización, no pelees dentro del modo automático.

## Los disparos: doctrina en el momento del gate

La IA no lee la doctrina. La doctrina le llega **compilada a disparos** — mensajes cortos (regla + porqué, autocontenidos) que se inyectan en el `permissionDecisionReason` de un hook, en un mensaje de CI o en la plantilla de un PR. Los 15 disparos viven en [`../kit/.jidoka/disparos/`](../kit/.jidoka/disparos/). Algunos clave:

- **`no-verify-es-teatro`** — saltarte el hook local solo pospone y agranda el fallo; el muro real es el required check.
- **`evidencia-no-palabra`** — el gate lee el artefacto (archivo, timestamp, diff, log), no la palabra del agente.
- **`prueba-de-vida-del-gate`** — ¿cuándo rechazó este gate algo real por última vez? Silencio = podrido.

## Prueba de vida

Un gate que nunca rechaza nada está podrido aunque el tablero esté verde. Por eso el motor Andon incluye un **self-test** (`probar-gate`): corre casos de resultado conocido —incluido uno que DEBE bloquear— para que la rama que bloquea no se pudra sin que nadie lo note. Quien valida, también se valida.

## Cuatro reglas de campo (pagadas en el linaje)

- **El gate gobierna hacia adelante; la historia tiene baseline.** Al estrenar un gate en un repo existente, el modo forense surfacea commits previos a la regla — eso no es deuda accionable: se limpia en un PR dedicado o se acepta como baseline. La autoridad del gate empieza el día que se enciende.
- **Hay una segunda familia de barreras: las que protegen el proceso, no el repo.** Corren donde el artefacto vive (el envío, el despliegue), no en CI. Mismas leyes: choke point, deny/ask, ledger de lo realmente enviado ("el registro se hace SOLO tras enviar de verdad"), y sin historial el check **se degrada a lo verificable hoy y lo dice** — no truena. Un `ask` que grita en falso seguido se recalibra o se poda: un gate ruidoso entrena el reflejo de click-para-pasar.
- **Un principio sin mecanismo es una promesa.** En el linaje, "nada sale del dispositivo" fue disciplina de código hasta que se volvió regla de plataforma (una CSP que lo hace imposible) — con la consecuencia deliberada de que violar el principio en el futuro exija revisitar el ADR a propósito. El costo de salida como diseño: poka-yoke aplicado a los propios ADRs.
- **Sin descartes silenciosos.** Cuando un motor decide omitir o callar algo, el descarte queda en el artefacto de auditoría con su razón. Caso real: unos `continue` tiraban datos sin registrarlos y el reporte *mentía por omisión*. El silencio también es auditable.

## El motor, en concreto (Sprint 1)

En este repo el motor son varias piezas en [`../tools/`](../tools/) más los hooks y el CI:

| Pieza | Qué hace |
|---|---|
| [`tools/blast-radius.json`](../tools/blast-radius.json) | La ley. 10 áreas (incluida `raiz`, la tierra de nadie — con sus canónicos reales excluidos, ADR 0038; `ritual`, los comandos `/jidoka:*` y las skills; `atlas`, el aviso proceso→diagrama del atlas — comando **y** script del motor desde el ADR 0038, que también acopla la dirección inversa vía `metodo`; `guias`, las guías operativas que viajan a los hijos; `barreras` incluye el CLI `bin/`; y `kit`, el instalador y el kit sembrable — Sprint 3). Dimensiones por área: `doc_bloquea` (**un** solo real: un ADR nuevo debe listarse en su índice), `doc_avisa`, **`product_avisa`** (el grafo de `product/`), **`revisa`** (marca el código que pide `/code-review`) y `rol` (`revisor-visual` enciende `gemba-stop`; `validador` enciende `validador-stop`). Los mensajes enseñan cuándo **no** aplican (anti-fatiga). |
| [`tools/verificar.ps1`](../tools/verificar.ps1) | El verificador. Corre local (pre-push) y en CI. **Avisa** los `doc_avisa`, **bloquea** los `doc_bloquea`, y **falla cerrado** (exit 2) si no puede medir. Incluye el salvavidas **`no-borres-el-motor`** (ADR 0032): si el diff **borra** una pieza del motor (`tools/*.ps1`, `tools/blast-radius.json`) y no hay un ADR nuevo en el mismo cambio, **BLOQUEA** (exit 1) — borrar sin decisión no pasa; con ADR, pasa. |
| [`tools/probar-gate.ps1`](../tools/probar-gate.ps1) · [`probar-hooks.ps1`](../tools/probar-hooks.ps1) · [`probar-auditor.ps1`](../tools/probar-auditor.ps1) · [`probar-agentes.ps1`](../tools/probar-agentes.ps1) | Los self-tests (prueba de vida). Casos de resultado conocido, cada uno con al menos uno que DEBE bloquear. Corren en CI sobre el motor del PR. `probar-gate` incluye además un **fixture del quickstart del README** (flujo real commit→verificar por git), para que la demo copy-paste no se rompa en silencio. `probar-agentes` es el lint de los **agentes-asiento tiereados** (`.claude/agents/`, ADR 0033): caza un `model:` inventado o ausente que caería en silencio al default caro. |
| [`tools/auditar.ps1`](../tools/auditar.ps1) | El **auditor del grafo** de docs (`product/`): frontmatter, wikilinks, Gherkin de las capacidades `vigente`, huérfanas — modulado por estado (`kanban/estados.md`). Cosechado de los casos de éxito (ADR 0007). Corre en CI `-Range base...HEAD -Bloquea`. |
| [`tools/anti-pii.ps1`](../tools/anti-pii.ps1) + [`probar-anti-pii.ps1`](../tools/probar-anti-pii.ps1) | El **detector anti-fuga de PII de entorno** (ADR 0041, disparo `sin-pii-en-el-repo`): en los docs rastreados de un repo público **BLOQUEA** (exit 1) las *formas* de dato personal — email con dominio real y rutas de perfil de usuario nombradas (`C:\Users\<x>`, `/home/<x>`) que no sean placeholders. Busca clases, no instancias (un gate que contiene la PII que busca la re-publica). Cinturón local opcional: `tools/anti-pii.denylist.txt` (gitignoreada, nunca committeada; plantilla `.example.txt`). Corre en CI **con el detector de la base** y local en el pre-push; **falla cerrado** (exit 2) si no puede listar el árbol. |
| [`tools/instalar.ps1`](../tools/instalar.ps1) + [`probar-instalador.ps1`](../tools/probar-instalador.ps1) | El **instalador** (Sprint 3, MVP Windows-first, ADR 0008/0009): siembra el método en un repo destino leyendo el motor genérico del árbol (sin duplicar la ley), regla **no-clobber**. **Pregunta el arquetipo** (`docs-as-code`/`code-first`) y siembra distinto — la matriz vive como manifiesto ejecutable (`kit/.jidoka/instalar/manifiesto.json`) con las leyes-plantilla (`kit/.jidoka/leyes/`). Su smoke instala en un repo temporal y corre los self-tests sembrados — un instalador que siembra un motor roto se caza ahí. |
| **El lazo labs↔Jidoka** (Fase 3.C, ADR 0012): [`tools/version.txt`](../tools/version.txt) + [`probar-version.ps1`](../tools/probar-version.ps1) · sello `tools/jidoka-motor.json` · `instalar.ps1 -Actualizar` · [`estado-motor.ps1`](../tools/estado-motor.ps1) · [`sembrar-manual.ps1`](../tools/sembrar-manual.ps1) + [`probar-sembrar.ps1`](../tools/probar-sembrar.ps1) · [`reportar-leccion.ps1`](../tools/reportar-leccion.ps1) | *La lección sube, la máquina baja.* `version.txt` es el **SSOT** (atado al CHANGELOG por `probar-version`); el instalador siembra el **sello** (versión + hash por pieza); **`-Actualizar`** re-siembra solo la mecánica con conciencia de tres vías (no pisa lo que el hijo customizó); **`estado-motor`** avisa si el hijo está atrás (aviso, no muro) **y degrada con gracia** apuntando al fallback si `instalar.ps1` no es legible; **`sembrar-manual.ps1`** es el **camino de siembra/actualización AV-seguro y completo, independiente de `instalar.ps1`** (para Windows endurecido con AV que lo pone en cuarentena — ADR 0027 + enmienda 2026-07-15: el trigger es densidad de comportamiento, no el nombre; siembra la **instancia entera** —mecánica + ley + stubs + sello—; su smoke `probar-sembrar` lo verifica); **`reportar-leccion`** abre el canal de subida. La costura `verificar.local.ps1` deja al hijo extender la mecánica sin bifurcarla. |
| **Gobierno documental por estructura** (KIT-2, ADR 0042): [`tools/docs-gobernados.json`](../tools/docs-gobernados.json) + [`estado-docs.ps1`](../tools/estado-docs.ps1) + [`probar-docs.ps1`](../tools/probar-docs.ps1) | El **hermano estructural del sello**: el motor se gobierna por **hash**, los documentos instancia-de-template que el ritual inyecta con `@` (`brief`/`infra`/`CONTRIBUTING`) por **secciones**. El ledger declara capa-1/2/3 y las secciones **requeridas** (congeladas) de cada capa-2; **`estado-docs.ps1`** verifica que estén (faltante → DESVIADO/*garantía nula*; aditiva → OK) — **aviso** en `/jidoka:arranca` (exit 0). Gobierno por estatuto: alterar la estructura gobernada = garantía nula. El **muro es opt-in y nace apagado**: `estado-docs -Estricto` bloquea (exit 1) solo si un doc marcado `estricto:true` en el ledger pierde una requerida, cableado en `andon.yml` (**nunca** en `verificar.ps1`, para no clobbear el verificar del hijo). El grafo de producto se queda con `auditar.ps1`; este gobierna solo los 3 singletons inyectados. `probar-docs` lo verifica (comportamiento + integridad del ledger). |
| **La conformidad de los ADRs** (ADR 0050): [`tools/probar-adrs.ps1`](../tools/probar-adrs.ps1) + el molde único [`docs/decisions/0000-plantilla.md`](../docs/decisions/0000-plantilla.md) ↔ [`kit/.jidoka/templates/adr.md`](../kit/.jidoka/templates/adr.md) | El **segundo muro real del repo** (antes solo bloqueaba el índice de ADRs). Valida que cada `docs/decisions/NNNN-*.md` traiga las **5 secciones canónicas** (Contexto/Decisión/Por qué/El camino que NO se toma/Consecuencias), que la **clase de estado del header** coincida con la del índice (el bug 0044: `aceptado` vs `reemplazado` se desincroniza en silencio) y que no haya **huérfanos** (archivo sin fila / fila sin archivo). `exit 1` ante cualquier desvío — **MURO** (decisión del cliente 2026-07-22: bloquea todo; el baseline se alinea primero, el muro nace verde), cableado en `andon.yml` (CI) y en el preflight de `publicar.ps1`. Trae su self-test sintético (caza sección faltante, estado incoherente y ambos huérfanos — quien valida se valida). **Sembrado** (mecánica): la disciplina de ADR baja con la máquina; mide **forma + coherencia de estado, no la verdad** de la decisión (el listón de contenido lo pone el humano). Causa raíz curada: la skill personal `adr-helper` que creaba ADRs vivía fuera del repo y había divergido (`Razones` vs `Por qué`). |
| **El contrato del HANDOFF** (FLU-1, ADR 0049): check `[contrato-handoff]` en [`tools/verificar.ps1`](../tools/verificar.ps1) + [`tools/flujo.json`](../tools/flujo.json) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | El **primer diente del pilar de flujo** (JIT): el relevo se **jala**, no se empuja. Mide el `HANDOFF.md` como invariante de **estado** (no de diff — se para aunque el push no lo toque): **una** sección «Dónde estamos», **máximo `max_historicas`** históricas («Dónde estuvimos»/«Antes») y **techo de líneas**. Los límites son **dato de instancia** en `tools/flujo.json` (hoy: 2 históricas, 120 líneas); cualquiera excedido → **BLOQUEA** el push (exit 1). Lo cerrado se archiva ÍNTEGRO en [`docs/handoff-historico.md`](../docs/handoff-historico.md), que **`/jidoka:arranca` nunca inyecta** (por eso el HANDOFF puede podarse sin perder la historia). **Sin `tools/flujo.json` (o sin la clave `handoff`) el check NO aplica** — un repo sin el pilar de flujo no se bloquea; el archivo presente pero **corrupto → falla cerrado** (exit 2). El HANDOFF lee con `-Encoding UTF8` a propósito (PS 5.1 sin eso deforma los acentos y mediría el contrato en falso). `probar-flujo` lo verifica (fixtures ROJO→VERDE: conforme con acentos UTF-8 contados bien, doble «estamos», históricas de más, techo excedido, sin flujo.json = no aplica, corrupto = falla cerrado). |
| **El contrato del ROADMAP** (FLU-1, ADR 0049): check `[contrato-roadmap]` en [`tools/verificar.ps1`](../tools/verificar.ps1) + [`tools/flujo.json`](../tools/flujo.json) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | El **hermano del contrato del HANDOFF**: el `ROADMAP.md` es una cola de trabajo **CLASIFICADA, no un diario**. Mismo carácter de invariante de **estado** y misma semántica de **falla cerrado**. Las únicas secciones `## ` legales son las **clases de servicio** —Urgente, Con fecha, Normal, «Algún día»— más **Referencia** (landscape, sin contrato de ítem); cualquier otra → **BLOQUEA** («el ROADMAP es cola clasificada, no diario»). Cada ítem vivo (`- ` de nivel raíz; los sub-bullets no cuentan) declara **`[alta:AAAA-MM-DD]`**; bajo Urgente/Con fecha/Normal además **`apetito:Nh`**, y bajo Con fecha además **`vence:AAAA-MM-DD`** — cualquier metadato faltante nombra el ítem y **BLOQUEA** (exit 1). Con **`roadmap.procedencia:true`** (opt-in de instancia, **ADR 0057**) cada ítem vivo además **cita su origen** —informe `docs/analisis/`, récord `docs/sprints/`, ADR (`docs/decisions/` o `ADR NNNN` textual) o `#issue`—; su ausencia **BLOQUEA** igual, aplica a **toda clase viva** (incluido el icebox), y off por defecto un hijo no adopta la regla al actualizar. Más un **techo de líneas** (`tools/flujo.json`, hoy 90) para que la cola no crezca a diario. El roadmap-diario anterior se archiva ÍNTEGRO en [`docs/roadmap-historico.md`](../docs/roadmap-historico.md). **Sin la clave `roadmap` en `tools/flujo.json` el check NO aplica**; presente pero **incompleta (sin `techo_lineas`) → falla cerrado** (exit 2). Lee con `-Encoding UTF8` (mismo gotcha de acentos que el HANDOFF). `probar-flujo` lo verifica (fixtures ROJO→VERDE: conforme con conteo, sin `alta:`, urgente sin apetito, «Con fecha» sin vence, sección extraña, techo excedido, Referencia exenta, sin clave = no aplica, incompleto = falla cerrado; procedencia r1p–r5p: con informe pasa, huérfano BLOQUEA, ADR/#issue cuentan, opt-in off no exige, icebox también). |
| **El contrato del CHANGELOG** (FLU-1, ADR 0049): check `[contrato-changelog]` en [`tools/verificar.ps1`](../tools/verificar.ps1) + [`tools/flujo.json`](../tools/flujo.json) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | El **tercer contrato del pilar de flujo**: el `CHANGELOG.md` es **registro operativo, no una carta entregable**. Mide **solo la sección TOPE** (del primer `## [` al siguiente) — **no es retroactivo**: las versiones viejas quedan como historia. La sección tope lleva **header datado** `## [X.Y.Z] — AAAA-MM-DD` (semver + fecha; guion largo o simple), **bullets tipados** — todo bullet raíz empieza con `- **` y un **tipo permitido entre backticks** (`feat`/`fix`/`test`/`docs`/`chore`/`breaking`) o la palabra `ADR ` (las decisiones se listan `- **ADR 0045** — …`, voz de la casa) — y **prosa con techo** (`max_prosa_lineas`, hoy 8; los `### ` no cuentan): header roto, bullet sin tipo o prosa excedida → **BLOQUEA** (exit 1). Los tipos y el techo son **dato de instancia** en `tools/flujo.json`. **Sin la clave `changelog` el check NO aplica**; presente pero **incompleta (tipos ausente/vacío o `max_prosa_lineas` no-entera) → falla cerrado** (exit 2). Lee con `-Encoding UTF8` (mismo gotcha del guion largo/acentos que el HANDOFF). `probar-flujo` lo verifica (casos z1–z8: tope conforme, header sin fecha, bullet sin tipo, `ADR ` aceptado, prosa de carta, segunda sección vieja no medida, sin clave = no aplica, incompleto = falla cerrado). |
| **El límite WIP + la vista de «qué sigue»** (FLU-1, ADR 0049): [`tools/estado-flujo.ps1`](../tools/estado-flujo.ps1) (`-Gate` / `-Json` / resumen) + `estado` (dato VIVO de instancia) en [`tools/flujo.json`](../tools/flujo.json) + [`.claude/hooks/flujo-sessionstart.ps1`](../.claude/hooks/flujo-sessionstart.ps1) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | **Tres modos, un molde.** **(1) `-Gate` (R5) — GATE de COMANDO, no de push** (no bloquea `git push` — eso es el `verificar`; bloquea **abrir un sprint nuevo**). El muro de entrada del pilar de flujo: *drum-buffer-rope*, el ritmo lo marca la **aceptación del cliente, no la producción**. `/jidoka:planea` corre `estado-flujo.ps1 -Gate` en su preflight; si hay un Gemba en `estado.gembas_pendientes` con `aceptado != true`, imprime `[BLOQUEA]` **nombrando el id** + `== ABRIR SPRINT NUEVO BLOQUEADO … ==` y sale **exit 1** — DETENTE, no se redacta R0. La aceptación se registra en `/jidoka:gemba` (`aceptado: true` + `aceptado_fecha`); el cierre (`/jidoka:cierra`) registra el Gemba nuevo (`aceptado: false`). Entrada malformada (sin `id`) → **AVISO + cuenta como pendiente** (fail-safe). **(2) `-Json` (R6) — la VISTA** que la cara Tauri pintará después (este motor **no dibuja UI**): emite a stdout SOLO el contrato JSON (`version`, `sprint_activo`, `wip_limite`, `gembas_pendientes`, los **3 `siguientes`** del ROADMAP —Urgente por alta, Con fecha por vence, Normal por alta—, `esperando_terceros` agrupables por `quien`, `muertos_recientes`, `conteos`). Parsea el ROADMAP con las convenciones de `[contrato-roadmap]` (título = negritas `**…**`). **(3) resumen (default, R6)** — el mismo estado en <15 líneas humanas, lo que se **empuja al abrir sesión**: el **primer hook `SessionStart` del repo** (`.claude/hooks/flujo-sessionstart.ps1`, cableado en `.claude/settings.json`) lo inyecta sin que nadie lo pida (gestión visual). Sin la clave `estado` (o sin `tools/flujo.json`) → «no aplica» / JSON mínimo; corrupto → **falla cerrado** (exit 2). El hook sin `tools/estado-flujo.ps1` sale 0 silencioso (un repo sin el pilar no ensucia el arranque). `probar-flujo` lo verifica (w1–w6 el gate; j1–j4 la vista: JSON completo, sin ROADMAP degrada, resumen imprime, corrupto = falla cerrado) y `probar-hooks` prueba la vida del hook. |
| **La expiración del ROADMAP** (FLU-1, ADR 0049): [`tools/expirar.ps1`](../tools/expirar.ps1) + `roadmap.vencimiento_dias`/`roadmap.muertos` en [`tools/flujo.json`](../tools/flujo.json) + [`docs/MUERTOS.md`](../docs/MUERTOS.md) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | **No es gate: es la poda automática** (mecánica que **EDITA**, no que juzga — el circuit breaker de Shape Up). Mueve los ítems **vencidos** del `ROADMAP.md` a `docs/MUERTOS.md` con **fecha y motivo**, por **clase de servicio** (`vencimiento_dias`: «Con fecha» muere si su `vence:` ya pasó; Urgente/Normal/«Algún día» si `alta + su ventana < hoy`; Referencia nunca). Lo caduco muere **por script, no por juicio** — la poda deja de depender de que alguien se acuerde; revivir = re-proponer con **alta nueva** (nada vuelve solo). **Paso duro del cierre** (`/jidoka:cierra` §2, ejecuta) y **early-warning en el arranca** (`-Simular`: anuncia lo que moriría y lo que vence en ≤7 días, sin tocar nada). Un ítem sin fecha parseable → **AVISO**, no se toca (el gate `[contrato-roadmap]` ya lo bloquea; expirar no duplica el muro). **Idempotente**; sin `roadmap.vencimiento_dias` → «no aplica» (exit 0); `flujo.json` corrupto → **falla cerrado** (exit 2). Preserva el resto del archivo byte-igual (UTF-8 sin BOM). `probar-flujo` lo verifica (casos s–y). |
| [`tools/rutear.ps1`](../tools/rutear.ps1) | El **router de la sesión** (ADR 0029). Lee la ley y reporta, de forma determinista, qué área se rutea a qué asiento, qué gate la vigila, y qué Stop hooks están **VIVOS o DORMIDOS** con la **razón** de cada dormido (fuente única de la lógica vivo/dormido; la misma que cada hook filtra). `/jidoka:arranca` lo adopta al abrir; `estado-motor -Gates` lo consume para hacer **visible la dormancia** (un gate dormido ya no sale limpio y en silencio — lección #51). **Falla cerrado** (exit 1) sin ley legible. Prueba de vida en `probar-hooks.ps1`. |
| **El reporte de avance para terceros** (FLU-1, ADR 0049): [`tools/reporte-avance.ps1`](../tools/reporte-avance.ps1) + [`probar-flujo.ps1`](../tools/probar-flujo.ps1) | La **VISTA para terceros**, hermana de `estado-flujo.ps1` y de la linterna: **NO es gate** (no bloquea nada, respeta ADR 0002), es **producto mecánico del estado real** — deriva del CHANGELOG (qué se cerró), del HANDOFF (el avance en un **hill chart** de Basecamp: subir = resolver incógnitas, bajar = ejecutar lo conocido, **sin porcentajes**), de `estado-flujo.ps1 -Json` (qué sigue, qué espera respuesta) y de `docs/MUERTOS.md` (qué se descartó) y los pinta en un **`.html` autocontenido** (cero deps/servidor, se abre con doble clic) de **5 secciones en lenguaje LLANO** — la jerga (`gate`/`WIP`/`commit`/`PR`/`rebanada`/`sprint`) se **traduce**, para que el cliente se lo mande **tal cual** a un no-técnico. **Degrada con gracia** si falta alguna fuente (sin `MUERTOS`/`flujo.json` → «Nada se ha descartado»); nadie lo redacta a mano. `probar-flujo` lo verifica (casos r1–r3: repo real con los 5 títulos y **cero jerga**, fixture mínimo que degrada, hill `<svg` presente). |
| [`tools/estado-gobierno.ps1`](../tools/estado-gobierno.ps1) + [`probar-linterna.ps1`](../tools/probar-linterna.ps1) | La **linterna del gobierno** (ADR 0043): una **vista de solo lectura** que deriva el grafo del gobierno de la ley real (`blast-radius.json` + `docs-gobernados.json` + `settings.json` + `andon.yml` + `product/capacidades`) y lo emite a un **`.html` autocontenido** (force-directed, JS vanilla; cero deps/servidor) que se abre con doble clic. Muestra áreas, gates (vivo/dormido), documentos-dueño (aristas **duras** `doc_bloquea` vs **blandas** `doc_avisa`), capacidades + wikilinks, hooks, checks de CI, y los **huérfanos en rojo** (lo que ninguna capa cubre) con su contador. **No inventa verdad** (matcher/reglas byte-fieles a `verificar`/`rutear`/`estado-docs`) y **falla cerrado** si el repo no es git. **Es vista, NO gate**: no bloquea nada, respeta ADR 0002; nace aviso por regla 2-3 (como el atlas). `probar-linterna` la verifica (fixture ROJO→VERDE + regresiones de inyección/falla-cerrado/acentos + `node --check` del JS). |
| [`tools/bandeja.ps1`](../tools/bandeja.ps1) + [`probar-bandeja.ps1`](../tools/probar-bandeja.ps1) | La **bandeja "pendiente de parametrizar"** (CFG-1, ADR 0045): una **vista de solo lectura** que reúne en UNA cola lo que ninguna regla gobierna de verdad, para que el verde deje de mentir. Tres motivos (matcher byte-fiel a `verificar`/`estado-gobierno`): **huérfano** (ninguna capa lo cubre), **solo existe** (vive en un árbol auditado sin regla — el hueco de `docs/`; `product/` NO cae aquí, lo gobierna el grafo de `auditar`), y **desviado** (doc capa-2 sin una sección requerida, misma detección que `estado-docs`). Resta lo firmado en `tools/contratos.json` (INSTANCIA, ADR 0046): `parametrizado` sale de la cola, `aceptado` sale con badge de firma. **Exit 0 siempre** (es cola, no muro; la UI la vacía en R4/R6); **falla cerrado** (exit 2) si no es git. `-Salida` emite un `.html` de tabla simple (UTF-8 sin BOM). `probar-bandeja` la verifica (15 casos: los 3 motivos, capa-2 conforme fuera, contrato/firmado fuera, no-git, HTML sin BOM, Parte B contra el repo real). |
| [`tools/ritual-gobernado.json`](../tools/ritual-gobernado.json) + [`estado-ritual.ps1`](../tools/estado-ritual.ps1) + [`probar-ritual.ps1`](../tools/probar-ritual.ps1) | El **estatuto del ritual** (CFG-1, ADR 0046): el hermano de `estado-docs` para los **`@`-includes de fábrica** de los comandos `/jidoka:*`. El ledger declara, por comando, sus `@` requeridos (`arranca`: brief/CONTRIBUTING/infra/HANDOFF; `planea`: HANDOFF/ROADMAP/brief; `que-sigue`: HANDOFF/ROADMAP; el resto con `[]` explícito — "no inyecta nada" deja de ser ambiguo). **Aditiva = OK** (un `@` extra del cliente es CONFORME); quitar un `@` de fábrica es **DESVIADO** nombrándolo (*garantía nula*). Extrae el token `@<ruta.ext>` fuera de fences (bare o backtickeado, como `que-sigue`). **Aviso** en `/jidoka:arranca` (exit 0); `-Estricto` = muro opt-in en CI, **nunca** en `verificar`. Trampa confesada (fase 1): un `@` legal hace DIVERGER el sello por hash aunque el estatuto sea CONFORME — el estatuto manda sobre la legalidad del `@`; R3b (clase `contrato` en la siembra) lo cierra. `probar-ritual` lo verifica (12 casos: conforme, faltante nombrado, aditiva, backtickeado, fence, `-Estricto`, Parte B real). |
| **Las ligas código↔capacidad** (ADR 0044): [`tools/ligas.json`](../tools/ligas.json) + [`estado-ligas.ps1`](../tools/estado-ligas.ps1) + [`probar-ligas.ps1`](../tools/probar-ligas.ps1) | El **gobierno granular que autora el cliente**: la extensión de VS Code (clic derecho → *"Jidoka: ligar a capacidad..."*) escribe el ledger — qué código sostiene qué capacidad, con **dirección** (`codigo-a-capacidad` / `capacidad-a-codigo` / `ambas`) y **fuerza** (`avisa` / `bloquea`) — y el gate determinista lo hace cumplir midiendo **co-ocurrencia** sobre el rango git (matcher byte-fiel a `verificar`). `[BLOQUEA]` se imprime siempre **nombrando la capacidad exacta** (mata el aviso grueso "revisa las 89"); solo `-Estricto` (pre-push + CI) lo vuelve exit 1. Ligas **ROTAS** (código o capacidad inexistente) avisan y quedan excluidas — nunca bloquean. **Falla cerrado** (exit 2) ante ledger ilegible. En CI, evaluador **y ledger se leen desde la base** (ADR 0003): un PR no puede desactivar la liga que lo juzga. El ledger es **dato de instancia** (NO se siembra; el evaluador y su test sí). `probar-ligas` lo verifica (26 casos: co-ocurrencia, dirección, rotas, falla-cerrado, contrato JS↔PS). |
| [`tools/asientos.ps1`](../tools/asientos.ps1) | El **casting vivo** de la sesión — el gemelo del router para los asientos-subagente. Lee `.claude/agents/*.md` (frontmatter `name`/`model`/`description`, ADR 0033) e imprime la tabla asiento→tier de forma determinista: `/jidoka:arranca` la imprime al abrir en vez de cargar una copia en prosa que deriva — la tabla se genera del artefacto con dientes, así que no puede mentir sobre él. **Degrada con gracia** (exit 0): sin agentes sembrados imprime `[DEGRADADO]` con el fallback (`general-purpose`, acusando el asiento) — el mensaje ES la acusación. Prueba de vida en `probar-hooks.ps1`. |
| `.claude/hooks/` + `.github/workflows/andon.yml` | Stop hooks locales: `andon-stop` (doc-drift), **`review-stop`** (código sin `/code-review` frena; marcador humano `.review-marker`) **`gemba-stop`** (cambio visual sin el `LOG.md` de la corrida en `qa_runs/<corrida>/LOG.md` frena — dormido si no hay áreas `rol: revisor-visual`) y **`validador-stop`** (spec/datos sin el `LOG.md` de una corrida de motor determinista en `qa_runs/validador-*/LOG.md` frena — dormido si no hay áreas `rol: validador`; la variante *medición* de Gemba para deliverables de datos/especificación, #52); + `no-memorias` y **`candado`** (PreToolUse). El **candado IA** (ADR 0047, disparo `deny-vs-ask`): deniega **en el momento** que la IA intenta editar (Write/Edit/Bash) una pieza con candado en `tools/contratos.json` — el único muro determinista del meta-gobierno (el `permissions.deny` es capa estática por prefijo, no por ruta). **Falla-abierta** si no hay ledger (viaja a hijos sin candados). Si git falla de verdad, **avisan** en vez de callar (ALTO-04). El check `andon` en cada PR — **con la ley y el verificador leídos desde la base** (ADR 0003) — y **sube los avisos al summary del PR** (cierra la grieta 1). |

### Encenderlo

1. **Hooks locales** (una vez por clon): `git config core.hooksPath .githooks` — así el `pre-push` corre el verificador antes de cada push. Los hooks de Claude (`no-memorias`, `andon-stop`) se cablean solos vía `.claude/settings.json`.
2. **El muro real** (paso humano, una vez): en GitHub → *Settings → Branches → Branch protection rule* de `main`, con **tres** cosas — sin las tres no hay muro:
   - **Require a pull request before merging** (si se puede pushear directo, el check nunca corre);
   - el check del workflow Andon como *required status check* (en el selector de GitHub aparece con su nombre de job: **`andon blast-radius (la ley)`**);
   - **Do not allow bypassing the above settings** (si el admin puede saltárselo, para el admin —y para el agente usando sus credenciales— sigue siendo una sugerencia).
3. **Probarlo**: corre `./tools/probar-gate.ps1` (debe salir verde). Para verlo bloquear de verdad: agrega un ADR en `docs/decisions/` sin listarlo en el índice y corre `./tools/verificar.ps1`.

## Fronteras del muro (honestidad)

Ningún muro es infinito; estos son los límites conocidos de este motor, dichos de frente (la doctrina exige fronteras explícitas, `doctrina/06`):

- **La ley que juzga un PR es la de la base, no la del PR** — eso cierra el hueco auto-referencial (un PR ya no puede vaciar la ley que lo juzga). El costo: un cambio legítimo a la ley rige a partir del *siguiente* PR.
- **El primer push de una rama nueva no se verifica localmente** (sin upstream, el pre-push solo ve el working tree). Lo cubre el CI en cuanto abres el PR.
- **"Tocar" el doc dueño incluye borrarlo — para docs.** El matching mide presencia en el diff, no que el doc siga existiendo. Borrar el índice de ADRs junto con un ADR nuevo pasaría el gate (y lo cazaría el humano en el PR). Para las **piezas del motor** (`tools/*.ps1`, `tools/blast-radius.json`) esto ya no aplica igual: borrarlas sin un ADR nuevo en el mismo cambio **BLOQUEA** de por sí (`no-borres-el-motor`, ADR 0032) — el caso que faltaba cubrir era justo el que la frase anterior daba por sentado (issue #73: un subagente borró una pieza del motor y el review pasó verde encima).
- **Sin branch protection completa (paso 2), todo lo anterior es teatro.** El gate local se salta con `--no-verify` a propósito y por diseño; el muro es el check requerido server-side, y solo si el admin tampoco puede saltárselo.
- **El marcador de revisión es válvula humana, no auto-firma.** `review-stop` y `gemba-stop` se despejan con un marcador SHA (`.claude/.review-marker`, `.claude/.gemba-marker`, gitignored) que **pone el humano** tras revisar; el hook verifica que el SHA sea el del diff real (lo que se cuela, se atrapa). Un agente no puede escribirse su propio pase sin que corresponda al artefacto.
- **`gemba-stop` exige el `LOG.md` de la corrida, rastreado por git y fresco, y por eso corre local.** "Evidencia válida" = **el `LOG.md`** de la corrida (`qa_runs/<corrida>/LOG.md`, plantilla `qa-log.md`) que `git ls-files` rastrea (forzado al índice con `git add -f`, porque `qa_runs/` está gitignoreado) **y** con mtime posterior al cambio visual — no cualquier archivo suelto. Lo del `LOG.md` es el **listón de evidencia** (ADR 0030): un `veredicto.txt` pelón satisfacía frescura+tracking pero se degradaba a una tabla vacía en campo — el gate mide presencia+frescura+tracking del `LOG`, su contenido lo juzga el humano. Lo del `git ls-files` cierra un Goodhart anterior (ADR 0013): un archivo que nunca se commitea satisfacía el mtime pero git no lo veía. El mtime pide `Stop` local (un `git checkout`/clone reescribe mtimes). Mismo listón en `validador-stop` (`qa_runs/validador-*/LOG.md`). En Jidoka nace **dormido** (no hay áreas `rol: revisor-visual`); su prueba de vida está en el self-test, no en el repo.
- **`no-memorias` cubre `Write|Edit` **y** Bash, pero el matcher de Bash es heurístico** (grieta 2 de la auditoría externa, cerrada en parte desde `v1.1.0`): el hook inspecciona `tool_input.command` y bloquea la **escritura** a la memoria (ruta de memoria + token de escritura: `Set-Content`/`Out-File`/`New-Item`/`cp`/`mv`/`tee`/redirección `>`); la **lectura/recall** no se bloquea. **Residual honesto**: aliases (`sc`/`ac`/`ni`) y rutas ofuscadas (base64, armadas por variable) evaden el matcher, y **no hay cobertura server-side** — la memoria es conducta del agente, no estado del repo, así que un check de CI no puede verla. Los caminos obvios ya no rodean el deny; los exóticos siguen siendo frontera. Prueba de vida en `probar-hooks.ps1` (casos Bash deny/allow).
