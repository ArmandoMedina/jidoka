# Roadmap — Jidoka

> **Contrato del ROADMAP (FLU-1, ADR 0049): esto es una cola de trabajo clasificada, no un diario.** Cada ítem vivo declara `[alta:AAAA-MM-DD · apetito:Nh]` (los «Con fecha» además `vence:`; el icebox «Algún día» solo `alta:`). Se **clasifica, no se rankea** (Anderson: ordenar el backlog es desperdicio). Lo cumplido vive en `CHANGELOG.md`; lo vencido **muere solo** a `docs/MUERTOS.md` (`tools/expirar.ps1`) y solo vuelve si alguien lo re-propone. El check `[contrato-roadmap]` de `tools/verificar.ps1` hace cumplir el formato y el techo (límites en `tools/flujo.json`). El roadmap-diario anterior, íntegro: [`docs/roadmap-historico.md`](docs/roadmap-historico.md). Norte sin cambio: **la disciplina en el robot, el juicio en el humano**.
>
> **Regla de procedencia (decisión del dueño, 2026-07-23):** todo ítem vivo **cita de dónde vino** — un informe de `docs/analisis/`, un ADR o un issue. Un pendiente que no puede decir su origen no se puede ejecutar ni validar, así que no se guarda. La poda del 2026-07-23 aplicó la regla hacia atrás: 32 ítems huérfanos se fueron a [`docs/MUERTOS.md`](docs/MUERTOS.md) con su texto íntegro (revivir = re-proponer con alta nueva). Hoy la regla es prosa; volverla gate es una tarjeta de esta misma cola. Evidencia y método: [`exploracion-procedencia-del-backlog-202607.md`](docs/analisis/exploracion-procedencia-del-backlog-202607.md).

## Urgente

*(vacío — nada urgente en la cola.)*

## Con fecha

- **Bajar el batch a los labs** `[alta:2026-07-20 · vence:2026-08-04 · apetito:4h · espera:ventana-labs]` — **rango real medido 2026-07-23: dos labs en `v1.9.0` y uno en `v1.23.0`**, y los dos viejos son pre-1.17: su sello **no registra arquetipo**, así que el instalador se salta los stubs y solo avisa. entisoft gana `estado-ligas`, la mecánica configurable y el pilar de flujo; los otros re-sellar + converger a namespaced. Decide antes: si el contenedor `jidoka/` entra primero, los labs migran una vez; si no, dos. Evidencia: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md). La ventana la abre el cliente (el lab lo trabaja otro agente — no pisar).

## Normal

- **El candado de procedencia del ROADMAP** `[alta:2026-07-23 · apetito:2h]` — que `verificar.ps1` exija a cada ítem vivo un puntero a informe/ADR/issue, igual que hoy exige `apetito:`; **pide ADR** (extiende el contrato del 0049 con un campo obligatorio nuevo). La regla la dictó el dueño el 2026-07-23 y se aplicó a mano ese día a 9 tarjetas; al no ser mecanismo, el hueco volvió al día siguiente. Sin gate, el ROADMAP se vuelve a llenar de pendientes sin origen. Medición, guion de revisión y lo NO medido: [`exploracion-procedencia-del-backlog-202607.md`](docs/analisis/exploracion-procedencia-del-backlog-202607.md).
- **Asiento de exploración con allowlist de rutas** `[alta:2026-07-23 · apetito:3h]` — restringir la escritura a `ROADMAP.md` + `docs/analisis/**` con un hook `PreToolUse` calcado de `candado-pretooluse.ps1` pero invertido (allowlist, no denylist); pide ADR (muro nuevo, hermano del 0047). **Mecanismo ya medido el 2026-07-23, con bloqueo real rojo→verde:** el payload trae `agent_type`/`agent_id` **solo** en subagentes, así que el asiento se reconoce por nombre y el hilo principal por la ausencia del campo — es asiento, no modo de sesión. Guion de revisión y lo que sigue sin medir: [`exploracion-allowlist-por-asiento-202607.md`](docs/analisis/exploracion-allowlist-por-asiento-202607.md).
- **Un hook que truena falla ABIERTO** `[alta:2026-07-23 · apetito:1h]` — medido en vivo el 2026-07-23: un `SyntaxError` en un hook `PreToolUse` dejó pasar la escritura que debía bloquear, sin ruido. Es el mismo modo de falla de la tarjeta de los 4 Stop hooks por otra causa (crash, no ley ausente), y aplica también a `candado-pretooluse.ps1`. Decidir si un hook del muro debe fallar cerrado y cómo se prueba que sigue vivo. Evidencia y conejo rojo: [`exploracion-allowlist-por-asiento-202607.md`](docs/analisis/exploracion-allowlist-por-asiento-202607.md).
- **Cada ítem del backlog declara cómo se revisa** `[alta:2026-07-23 · apetito:2h]` — regla del dueño (2026-07-23): un pendiente sin guion de revisión no es ejecutable. **Medido el 2026-07-23 sobre el estado final: 20 de 41 ítems vivos no alcanzan ningún guion**, y de los 19 informes de `docs/analisis/` solo 5 tienen la sección «Qué debe revisar el dueño». El guion no cabe en la tarjeta (techo de 90 líneas), así que la regla es *la tarjeta apunta a un guion que vive en su informe* — extiende el candado de procedencia con un segundo requisito. Origen: [`exploracion-allowlist-por-asiento-202607.md`](docs/analisis/exploracion-allowlist-por-asiento-202607.md).
- **El casting canon (y el asiento que falta)** `[alta:2026-07-23 · apetito:2h]` — volver canon el reparto en el eje **neutral**: el método **no tiene rol de descubrimiento/exploración** y hoy ese trabajo se hace sin carta, sin límites escritos y sin gate; la tabla de ejemplo (`kanban/roles.md:40-48`) lo mapea a `arquitecto-doc`, que es formato del grafo de docs, no punta de lanza. Es asiento nuevo, no renombre; falta también nombrar al escribano en esa tabla. **Decisión del dueño (2026-07-23): la capa de nombres de pila vive FUERA de git** (patrón `plan-actual.md`, ADR 0006) porque son personas reales y aplica `CONTRIBUTING.md:40`. Medición y guion: [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **Nombres de personas ya publicados en `main`** `[alta:2026-07-23 · apetito:2h]` — seis nombres de pila viven en `kanban/roles.md:40-48` **desde el 2026-07-10** como tabla de «ejemplo», y son personas reales (confirmado por el dueño el 2026-07-23). Choca con `CONTRIBUTING.md:40` / ADR 0055. Decidir: reescribir la historia para sacarlos (el precio ya se pagó una vez, el 2026-07-22) o aceptarlos por escrito con un ADR que enmiende el 0055. **Hoy el repo se contradice a sí mismo**, que es el peor estado. Contexto: [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **Exploración profunda: el modelo de asientos está enredado (agentes vs skills vs casting)** `[alta:2026-07-23 · apetito:4h]` — **el dueño lo marcó importantísimo el 2026-07-23 y pidió una sesión aparte para revisarlo a fondo.** Sacado a la luz al preguntar si los 4 agentes eran fijos de Claude (no lo son: son archivos de `.claude/agents/`, editables/borrables, creados por este repo en `25681c1`). Huecos ya detectados que la exploración debe cerrar: (1) la doctrina del casting (`kanban/roles.md:31-48`) **solo documenta renombrar skills**, no agentes — el eje de agentes no tiene mecanismo escrito de personalización; (2) el método llama «asiento» tanto a agentes (se delegan, cabeza propia, tier fijo) como a skills (se aplican, sin cerebro propio) y eso confunde; (3) `explorador` como agente choca con «exploración» (la kata), con el asiento de punta de lanza que falta, y con el `Explore` de fábrica de Claude; (4) la punta de lanza que el dueño quiere no está definida como agente o skill; (5) falta un casting default neutral (instrumentos) que no choque con personas reales ni con oficios. Es una vuelta profunda de la kata, no un renombre suelto. **El mapa para arrancarla ya está levantado** (3 subagentes ciegos: localización, coherencia doctrinal, mecanismo real de la CLI): el renombre toca 120+ sitios con listas hardcodeadas (`probar-agentes.ps1:49` y gemelos) que lo vuelven un sprint con censo; varios «límites de plataforma» resultaron ser decisiones de este repo; y queda sin decidir si la punta de lanza es agente o skill. Insumo: [`exploracion-modelo-de-asientos-202607.md`](docs/analisis/exploracion-modelo-de-asientos-202607.md) y [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **La doctrina del casting no cubre el eje de agentes** `[alta:2026-07-23 · apetito:1h]` — `kanban/roles.md:38` define personalizar el casting solo como «renombras la carpeta del skill y su `name:`»; no hay una línea sobre nombrar un agente de `.claude/agents/`. El mecanismo funciona igual (cambiar `name:`, **nunca `model:`** — el tier es maquinaria, no etiqueta), pero está sin escribir. Sub-ítem de la exploración profunda de arriba; se puede cerrar solo si aquélla se pospone. Contexto: [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **Renombrar la mecánica por tier para que no suene a rol** `[alta:2026-07-23 · apetito:1h]` — `explorador` es el asiento **más barato** (haiku, barre y localiza) mientras «exploración» es el trabajo de **más juicio**: misma raíz, sentidos opuestos. Y `arquitecto` existe a la vez como tier (eje de esfuerzo) y como `arquitecto-doc` (rol del método). El eje 2 debe hablar de esfuerzo o de instrumento, no de oficio. **Candidatos a decidir:** (a) **instrumentos** — `sonda`/`pinza`/`báscula`/`brújula`: cada nombre dice qué hace y ninguno suena a puesto; (b) **pesos** — `pluma`/`ligero`/`medio`/`pesado`: el nombre ES el tier, no hay que memorizar el mapeo, pero no distingue los dos asientos que comparten tier; (c) **arbitrarios** (autos, letras): cero colisión pero hay que memorizar cuál es cuál — reintroduce el defecto de hoy. **No tocar el eje 1**: sus tokens los leen `blast-radius.json` y los hooks. **Antes de renombrar hace falta el censo de referencias** a `explorador`/`arquitecto` en skills, docs, atlas y suites, o el renombre rompe punteros. Medición: [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **Alcance de escritura por asiento (qué archivos toca cada quien)** `[alta:2026-07-23 · apetito:2h]` — generalizar la allowlist de `PreToolUse` de un asiento a todos: que cada uno declare sus rutas y el hook las haga cumplir. **Es el mismo mecanismo que la tarjeta del asiento de exploración, en otro alcance (todos vs uno)** — decidir si se fusionan antes de construir, o se duplica el esfuerzo. La medición ya está hecha: [`exploracion-allowlist-por-asiento-202607.md`](docs/analisis/exploracion-allowlist-por-asiento-202607.md). Origen: [`exploracion-casting-vs-mecanica-202607.md`](docs/analisis/exploracion-casting-vs-mecanica-202607.md).
- **El molde de exploración no carga las cinco preguntas de la kata** `[alta:2026-07-23 · apetito:1h]` — `kit/.jidoka/templates/exploracion.md` nació del capítulo `doctrina/08-kata-de-mejora.md` pero dejó fuera su cuestionario: **no pide condición objetivo, ni obstáculo único elegido, ni hipótesis antes de moverse, ni cita de revisión**. Medido en vivo el 2026-07-23: una exploración siguió el molde al pie de la letra y aun así se saltó la kata. El molde está en n=1 y fuera del ledger, pero **ya se siembra a todos los hijos**: hoy distribuye el atajo. Evidencia: [`exploracion-procedencia-del-backlog-202607.md`](docs/analisis/exploracion-procedencia-del-backlog-202607.md).
- **Contenedor `jidoka/` — aislar la maquinaria del repo anfitrión** `[alta:2026-07-22 · apetito:1h]` — el cliente ya lo enmarcó el 2026-07-22 («huella mínima en el hijo»: ~7 carpetas visibles hoy; la cura es el patrón `.git`/`.vscode`, todo bajo una carpeta) y el 2026-07-23 se midió. La forma **aplanada** pasa el gate sin tocar una línea; la **anidada** da falso-verde silencioso (resuelve mal la raíz y aprueba sin mirar nada); el instalador falla cerrado ante un lab migrado a mano. Extiende a carpetas el principio del ADR 0023; el 0024 no lo bloquea. Evidencia y lo aún NO medido: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **`review-stop` le entrega al agente la llave de su propia cerradura** `[alta:2026-07-23 · apetito:1h]` — su encabezado declara «**NO es auto-firma: el humano lo pone tras revisar**» (`review-stop.ps1:7-8`), pero el mensaje de bloqueo **imprime el comando exacto** para firmarlo (`review-stop.ps1:80`). Ocurrió en vivo el 2026-07-23: el agente se auto-firmó el marcador y siguió (revertido al detectarse). **Segundo defecto medido el mismo día:** su SHA se calcula sobre `git diff HEAD` (`review-stop.ps1:65-69`), así que **el contenido de los archivos sin rastrear no entra en el hash**. Contexto y evidencia: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **Tres scripts del motor resuelven rutas contra el CWD, no contra la raíz** `[alta:2026-07-23 · apetito:1h]` — `estado-flujo.ps1:102-105` y `expirar.ps1:79-82` prueban `'tools/flujo.json'` literal, y `auditar.ps1:82` fija `'tools/blast-radius.json'`: los tres aceptan `-Repo` y no lo usan en esas líneas, así que **ya fallan hoy** si se corren desde otra carpeta. Contexto: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **El contrato del apetito no expresa menos de una hora** `[alta:2026-07-23 · apetito:1h]` — el check es `apetito:\d+h` (`tools/verificar.ps1:300`), así que todo ítem chico se declara `1h` y el backlog **sobreestima el presupuesto de atención del dueño** — que es la restricción del sistema, no un detalle cosmético. Ejemplo medido: 3 tarjetas de ~45 min reales aparecen como 3h. Arreglo de una línea + su prueba; contexto: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **Los 4 Stop hooks dejan cerrar sin la ley** `[alta:2026-07-23 · apetito:1h]` — `andon`/`review`/`gemba`/`validador-stop` salen `exit 0` si falta `tools/blast-radius.json` (medido A/B: con ley `decision:block`, sin ley silencio y deja cerrar); `candado-pretooluse.ps1:26` documenta su falla-abierta y ellos no. Decidir si es diseño (y escribirlo) o defecto (y curarlo): [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **El lab DEBE poder usar la app (hoy no puede)** `[alta:2026-07-23 · apetito:1h]` — **decisión del cliente (2026-07-23): es obligatorio que la app llegue a los labs**; si toda superficie del gobierno debe ser la app, un lab sin ella es un lab sin superficie. **Cómo, sin firmar:** el agente propone que viaje el **motor** (`tuberia-datos`/`parametrizar`/`override`, hoy no sembrados) y no `app/`, porque la app es de escritorio y opera sobre la carpeta que se le indique (`app/src-tauri/src/lib.rs:33-36`). **El cliente no ha avalado ese mecanismo:** si prefiere la app dentro de cada lab, cambia el alcance y el ADR 0048. Guion: [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md).
- **Atlas de los tools nuevos de la app** `[alta:2026-07-21 · apetito:1h]` — `tuberia-datos`, `parametrizar`, `override` sin diagrama en el atlas. Son los mismos tres que **no se siembran** y dejan al lab sin poder usar la app (ver esa tarjeta; medición en [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md)).
- **Corte «estable 2.0» — promesas alineadas a la realidad** `[alta:2026-07-22 · apetito:5h]` — la lista cerrada del escaneo: `package.json` os a `["win32"]`, reconciliar la promesa `npx` del README (publicar o recortar), aviso ruidoso post-instalar de que branch protection es manual, badges a la verdad. Con esto el kit se instala en repos Windows ajenos sin miedo. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **Self-test anti-drift de las copias gemelas** `[alta:2026-07-22 · apetito:4h]` — `Test-Pattern` vive 8 veces byte-idéntico (verificar/estado-ligas/estado-gobierno/bandeja + 4 Stop hooks), parser del ROADMAP ×3 (si divergen, `expirar` borra mal) y `Clase-Display` **ya divergió** en claves. La duplicación es doctrina (standalone); la cura es un `probar-*` que falle si las copias divergen, no DRY. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **El salvavidas `no-borres-el-motor` no cubre los hooks** `[alta:2026-07-22 · apetito:1h]` — `verificar.ps1:164` protege `tools/*.ps1` y la ley, pero borrar un Stop hook, `settings.json` o `.githooks/*` no dispara nada. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **La UI dice la verdad del gobierno por documento** `[alta:2026-07-22 · apetito:6h]` — `tuberia-datos.ps1` no lee `docs-gobernados.json` (confHoy/confVision vacíos) y ROADMAP/HANDOFF/CHANGELOG salen como «libre» teniendo muro real en `flujo.json`; consolidar los 4 ledgers en la vista por documento. Prerequisito del objetivo «revisar doc por doc con la UI». Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **Parametrizar secciones desde la UI (el radio hoy es teatro)** `[alta:2026-07-22 · apetito:6h]` — marcar «estructura gobernada» escribe `contratos.json` pero el guardián de secciones lee `docs-gobernados.json`: ninguna sección se vigila. Falta `-Requeridas` en `parametrizar.ps1` + re-clasificar `docs-gobernados.json` a no-clobber (exige ADR). Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **La app absorbe el mapa de enforcement (y la linterna se retira)** `[alta:2026-07-22 · apetito:4h]` — decisión del cliente (2026-07-22): toda superficie del gobierno debe ser la app. La pantalla que enseña qué bloquea/avisa/duerme por área (la verdad de `rutear`/la ley); al existir, retirar `estado-gobierno.ps1` del motor con su ADR (supersede 0043) y limpiar suites/manifiesto/gemelas. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **Matriz de carriles de la IA (muro/prosa/nada)** `[alta:2026-07-22 · apetito:2h]` — publicar los 11 casos de uso como doc vivo: qué tiene muro probado, qué es prosa medida, qué nada; insumo para decidir qué merece cablearse (regla 2 de `kanban/roles.md`) sin sobre-cablear. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **`conformidad-docs.html` — el tablero sin terminal del molde** `[alta:2026-07-22 · apetito:2h]` — el paso 5 del Gemba del molde (CA-4/CA-6) prometía un HTML doble-clic espejo del `conformidad-adrs.html`; hoy el reporte CONFORME/DESVIADO por familia solo sale por terminal (`estado-docs.ps1`). Generar el tablero autocontenido. Origen: [`escaneo-camino-2.0-202607.md`](docs/analisis/escaneo-camino-2.0-202607.md).
- **«Última validación del dueño» como propiedad de cada pieza** `[alta:2026-07-22 · apetito:4h]` — decisión del cliente (2026-07-22): revisar y comprender lo existente es el trabajo; cada pieza carga la fecha de su última validación por el dueño, la app la muestra y la actualiza desde la vista por documento. Mide el avance de comprensión y delata lo nunca revisado. Origen: [`senales-tableros-202607.md`](docs/analisis/senales-tableros-202607.md).
- **Coordinación de escritores multi-máquina** `[alta:2026-07-21 · apetito:8h]` — 3 frentes de escritura sobre el método + la rama del socio sin subir; «una sola sesión escritora por working tree» no alcanza con varias PCs. Origen (decisión abierta #3): [`gemba-gestion-del-flujo-202607.md`](docs/analisis/gemba-gestion-del-flujo-202607.md).
- **Gemba end-to-end de la app de la tubería** `[alta:2026-07-21 · apetito:1h · espera:cliente]` — el flujo del glosario completo (crear por fuera → bandeja → parametrizar desde el formulario → candado → ver a la IA rebotar), sin código ni terminal; pasos en [`sprint-21-app-tuberia-entrega.md`](docs/sprints/sprint-21-app-tuberia-entrega.md). La fidelidad de R2 ya se aprobó; el flujo completo no se ha corrido.
- **Cuadro de cierre como plantilla sembrable** `[alta:2026-07-17 · apetito:2h]` — mover el detalle a `kit/.jidoka/templates/cierre-cuadro.md` inyectada con `@` (ADR 0040).
- **Épica `.local` code-first (SGI)** `[alta:2026-07-11 · apetito:8h · espera:ventana-labs]` — converger el `verificar` de SGI a motor genérico + costura `.local` sin romper sus 453 tests (ADR 0015).
- **Demo de campo de Discovery** `[alta:2026-07-14 · apetito:2h · espera:cliente]` — correr `/jidoka:descubre` en un proyecto con niebla real; alimenta el issue #67.

## Algún día

- **Arquetipo «exploración» + graduación a producción** `[alta:2026-07-22]` — el instalador pregunta el modo: producción (disciplina completa) o exploración barata (gates en aviso, sin ADRs/contratos). Cada exploración lleva su pregunta escrita; la graduación es acto explícito. **La evidencia que esperaba ya llegó** (2026-07-23): la fricción real está medida en [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md) y el molde de la vuelta quedó en `kit/.jidoka/templates/exploracion.md` (n=1, sin ledger — **pero el folder se siembra completo**: decidir si se queda o sale hasta tener 2-3 vueltas). Regla 2-3: faltan 2 vueltas antes de instrumentar mecanismo.
- **Empaquetar la app como instalable (.exe) para terceros** `[alta:2026-07-22]` — la app viaja a la máquina de la persona, no al repo (patrón Excel/VS Code): el compañero instala el `.exe` y apunta a su repo con el kit. El NSIS «de R7» + firma; se prioriza con la señal del lab. Confirma el encuadre medido en [`exploracion-huella-en-labs-202607.md`](docs/analisis/exploracion-huella-en-labs-202607.md): lo que viaja al repo es el motor, no `app/`.
- **Arquetipo `doc-only`/regulado** `[alta:2026-07-13]` — regla 2-3: 1er uso real llegó (PLD/CNBV); espera el 2º repo regulado (#41).
- **Arquetipo `operacion`** `[alta:2026-07-13]` — doctrina sin motor; 1er caso real (Caso F); espera el 2º (#44).
- **Gobernanza compuesta** `[alta:2026-07-13]` — 2º apunte del patrón; espera el 3º (#45).
- **Prueba de vida ≠ tests verdes** `[alta:2026-07-13]` — leading indicators de gates de operación; espera el 2º caso (#46).
- **#47 pre-push no protege la rama default** `[alta:2026-07-13]` — sin triar; se decide en la próxima cosecha.
- **Publicar la doctrina suelta «Poka-yoke»** `[alta:2026-07-09]` — el ADR 0001 lo deja abierto; decide el autor.

## Referencia

> Landscape y declaraciones — no son cola de trabajo; no llevan contrato de ítem.

- **Frontera Core vs familias opcionales (issue #71):** Core estable (memoria por artefactos, gates fuera del LLM, ritual, instalador+lazo) · Discovery (1 caso real) · Docs/`doc-only`, Operations, Observability: esperan consumidores reales (regla 2-3). Detalle histórico en `docs/roadmap-historico.md`.
- **Panorama — OpenWiki** (LangChain, MIT): wiki para agentes, flecha código→doc generativa; complemento, no competidor (la nuestra es normativa doc→código con gate).
- **Panorama — GBrain** (Garry Tan, MIT): grafo de conocimiento git-nativo con consulta citada vía MCP; interés como capa «pregúntale al proyecto», no gobierna nada.
