# Decisiones (ADRs) — Jidoka

Registro de las decisiones de arquitectura del proyecto. Cada ADR captura **una** decisión y su porqué, para que el contexto viaje entre sprints sin depender de la memoria de nadie.

> **Este índice es un doc dueño con bloqueo real.** La ley (`tools/blast-radius.json`, área `decisiones`) hace que agregar un ADR sin listarlo aquí **detenga el push**: un ADR fuera del índice es una decisión invisible. Es el único `doc_bloquea` del manifiesto (alto valor, baja fatiga).

| # | Título | Estado |
|---|---|---|
| [0000](0000-plantilla.md) | Plantilla — con la sección "El camino que NO se toma" | plantilla |
| [0001](0001-la-fusion-jidoka.md) | Jidoka: fusión de la doctrina, el método y el ritual de sprint | aceptado |
| [0002](0002-motor-andon.md) | El motor Andon: gates deterministas sobre el propio Jidoka | aceptado |
| [0003](0003-auditoria-del-motor.md) | La auditoría del motor: falla cerrado, el juez viaja en la base, y el nombre real | aceptado |
| [0004](0004-centralizacion-del-conocimiento.md) | Centralización del conocimiento del linaje: qué asciende ya, qué espera con registro, qué no asciende | aceptado |
| [0005](0005-exprimido-final-del-linaje.md) | El exprimido final del linaje: última cosecha, archivado de los repos de método, los casos de éxito siguen vivos | aceptado |
| [0006](0006-plan-efimero.md) | El plan de trabajo: efímero, con hogar persistente fuera de git (`/.jidoka/plan-actual.md`) | aceptado |
| [0007](0007-homologacion-de-los-muros.md) | La homologación de los muros: `review-stop`, `gemba-stop` y el auditor del grafo, cosechados de los casos de éxito | aceptado |
| [0008](0008-el-instalador.md) | El instalador mínimo: PowerShell-first, siembra leyendo del árbol sin duplicar, un arquetipo (docs-as-code) | aceptado |
| [0009](0009-arquetipos-ejecutables.md) | Los arquetipos ejecutables (matriz-como-manifiesto) y la poda a dos (docs-as-code + code-first; doc-only diferido) | aceptado (delegado · revisable) |
| [0010](0010-roster-y-modo-desatendido.md) | El roster completo (`devops`), el modo desatendido general, y el casting neutral+persona (Homologación Etapa 1) | aceptado |
| [0011](0011-homologacion-cosecha-sgi.md) | Cosecha de SGI: token neutral en la ley (`escribano` en minúscula) y tres maduraciones al casting (Homologación Etapa 2) | aceptado |
| [0012](0012-lazo-sincronizacion-labs.md) | El lazo de sincronización labs↔Jidoka: sello de versión + `-Actualizar` (tres vías por hash) + aviso de divergencia + canal de subida | aceptado |
| [0013](0013-primera-cosecha-por-el-lazo.md) | Primera cosecha por el lazo: tres lecciones de campo absorbidas (gemba-stop rastreado, excepción de dominio con nombre, criterio de delegación) | aceptado |
| [0014](0014-listo-para-1.0.md) | Jidoka listo para 1.0: cerrar los bloqueantes de "corre en un repo ajeno" (instalador pregunta arquetipo, sembrar el método, fixture del quickstart, guía empezar-de-cero) | aceptado |
| [0015](0015-segunda-cosecha-por-el-lazo.md) | Segunda cosecha por el lazo: el mecanismo probado en producción (2 labs bajados) y las cuatro lecciones que suben (sello pristina-vs-customizada, estado-motor por-hash, drift estructural, épica `.local` code-first) | aceptado |
| [0016](0016-licencia-mit-consciente.md) | Licencia: MIT para máxima adopción (decisión consciente, no heredada; el camino copyleft no tomado) | aceptado |
| [0017](0017-jidoka-1.0.md) | Jidoka 1.0.0: el criterio de "corre en un repo ajeno", cumplido con evidencia (2 labs, 2 lenguajes, CI verde server-side) | aceptado |
| [0018](0018-muro-grietas-2-5.md) | El muro cumple lo que promete: grietas 2 (`no-memorias` cubre Bash) y 5 (registro de disparos cableados, testeable) cerradas con invariantes | aceptado |
| [0019](0019-lazo-ve-la-divergencia.md) | El lazo ve la divergencia: `instalar.ps1 -Sellar` (sello bootstrap clasificador pristina-vs-customizada) + `estado-motor -Detallado` (divergencia por-hash) | aceptado |
| [0020](0020-release-derivado-del-ssot.md) | El release se deriva del SSOT: `publicar.ps1` corta el tag+notas desde `version.txt`+CHANGELOG y corre la suite antes de publicar (Jidoka-only) | aceptado |
| [0021](0021-lazo-agnostico-al-eol.md) | El lazo es agnóstico al fin de línea: `Get-MotorHash` normaliza a LF (un hijo `eol=lf` reconcilia por contenido, no por bytes) — defecto cazado al bajar a TF | aceptado |
| [0022](0022-lista-de-exclusion-del-hijo.md) | La lista de exclusión del hijo: el sello declara `excluir: [rutas]` y el lazo no las re-agrega — mata el back-out recurrente (drift estructural, ADR 0015 #3) | aceptado |
| [0023](0023-estructura-canonica.md) | La estructura canónica: comandos namespaced (`/jidoka:*`, seguro de colisión), el rol neutral es el mecanismo, el nombre del skill es sabor de instancia (persona OK) — cierra el drift estructural | aceptado |
| [0024](0024-el-motor-se-lee-del-arbol.md) | El motor se lee del árbol (no "solo en kit/"): cierra la decisión abierta del ADR 0003 — no hay duplicación que eliminar, migrar la crearía, el dogfood ya lo cubre `probar-instalador` | aceptado |
| [0025](0025-git-idioma-nativo.md) | El substrato es git porque es el idioma nativo del agente: una API se degrada con el agente, git no (sincronizar = lógica de registry, interfaz de git) — cierra el porqué-git del ADR 0008, con el experimento de 3 agentes frescos como evidencia | aceptado |
| [0026](0026-cosecha-brownfield.md) | Cosecha brownfield: el instalador no era consciente de la instancia ni del arquetipo del hijo — cinco arreglos (#34–#38) cazados al bajar el método a repos reales (SGI, TequiOBD) | aceptado |
| [0027](0027-ruta-de-actualizacion-no-cuelga-del-instalador.md) | La ruta de actualización no cuelga de que `instalar.ps1` sea legible: `sembrar-manual.ps1` (independiente) + `estado-motor` degrada con gracia — cazado en repos regulados Windows con AV (#40/#43). **Enmienda 2026-07-15:** el trigger es densidad de comportamiento, NO el nombre; `sembrar-manual` se promueve a instalador AV-seguro **completo** (siembra la instancia entera); firma = cura de fondo | aceptado |
| [0028](0028-gate-de-validacion-por-medicion.md) | El gate de validación por medición (`validador-stop`): tercer gate de evidencia, para datos/especificación — un motor determinista recalcula contra golden-masters y la evidencia rastreada por git cierra el "validado al centavo" en prosa; variante local para fixtures confidenciales (#50/#51/#52) | aceptado |
| [0029](0029-conciencia-instalada-arranca-sienta-y-rutea.md) | La conciencia se instala: el `arranca` sienta (casting sembrado) y rutea (`rutear.ps1`, router determinista vivo/dormido), y la dormancia se hace visible en `estado-motor` — nada de conciencia depende de la iniciativa del agente (cierra #53/#51) | aceptado |
| [0030](0030-liston-de-evidencia-y-demo-que-corre-el-cliente.md) | El listón de evidencia (el `LOG.md` de la corrida, no cualquier archivo) que cierra el Goodhart de `gemba-stop`/`validador-stop`, y el demo que corre el cliente sin código ni terminal (disparo `demo-que-corre-el-cliente`) — el gate mide presencia+frescura+tracking, el contenido lo juzga el humano | aceptado |
| [0031](0031-capa-de-descubrimiento.md) | La capa de descubrimiento (`/jidoka:descubre`): la consultoría se instala como entrevista mecánica — diagnóstico de 3 nieblas, filtro Mom Test escrito, kit portátil para la autoridad tercera que no opera la IA, disparo `aprobacion-nombrada`, y ruteo desde `planea` — el QUÉ vive en ejemplos, STOP no es comprensión | aceptado |
| [0032](0032-el-motor-no-se-borra-sin-decision.md) | El motor no se muta en silencio: preflight de `publicar.ps1` falla cerrado ante un test ausente (issue #78) y el salvavidas `no-borres-el-motor` en `verificar.ps1` bloquea borrar una pieza del motor sin un ADR nuevo en el mismo cambio (issue #73) — el juez que no midió no aprueba, disparo `no-borres-el-motor` | aceptado |
| [0033](0033-tiers-como-agentes-asiento.md) | Los tiers de modelo se instalan como agentes-asiento (`.claude/agents/`: explorador/mecanico→haiku, auditor→sonnet, arquitecto→opus, con `model:` fijo en frontmatter) + lint `probar-agentes.ps1` en el preflight — la regla deja de vivir en prosa que nada obliga a usar (issue #63) | aceptado |
| [0034](0034-arranca-inyecta-no-encarga.md) | El `arranca` inyecta, no encarga (payload `@` directo: HANDOFF, brief, infra, CONTRIBUTING); el asiento lo ocupa el subagente (reframe rol-teatro: roster de responsables + router como preview de gates); y el brief se separa del CÓMO-operativo (`product/PRODUCT_BRIEF.md` + `product/infra.md`, issue #75) · **enmienda 2026-07-17:** el orden de lectura se reordena a `git → qué → cómo → dónde` (git primero como filtro de frescura) | aceptado |
| [0035](0035-atlas-de-procesos-bpmn.md) | El atlas de procesos vive en BPMN (`docs/atlas/`): el método se documenta como diagrama navegable — BPMN gana el bake-off de 3 formatos por fidelidad de swimlanes (agente/humano), toolchain Node on-demand (render/validar/layout), convención BPMN-formal + Mermaid-borrador; el costo de coordenadas se mitiga con autolayout | aceptado |
| [0036](0036-acoplamiento-proceso-docs-diagrama.md) | El acoplamiento proceso↔docs↔diagrama es asimétrico: el proceso (el `.md` del comando) es la fuente, prosa y diagrama son dos vistas, el código es implementación — se cablea un **aviso** (área `atlas`, tocar un comando `/jidoka:*` avisa revisar su diagrama), NO el acoplamiento simétrico de tres vías ni un bloqueo (se gana por regla 2-3) | aceptado |
| [0037](0037-la-conciencia-viaja-en-el-kit.md) | La conciencia viaja en el kit (cura B del #82): los agentes-asiento y su lint se siembran, la instancia que el `arranca` inyecta es stub común, `-Actualizar` migra lo que el motor nuevo asume (`[MIGRA]`, sello con `producto`/`gobernanza`), y el roster vive donde se inyecta (`## El casting` en `infra.md`) — cosecha #7, issues #86/#87 (nació 0035; renumerado al reconciliar con el atlas) | aceptado |
| [0038](0038-la-ley-gana-cobertura.md) | La ley gana cobertura (auditoría 2026-07-16, issue #97): el atlas se acopla en ambas direcciones (`tools/*.ps1` avisa al diagrama; `docs/atlas/*` avisa CHANGELOG vía `metodo`), `bin/` entra a `barreras`, área nueva `guias` (viajan a los hijos), y la raíz excluye sus canónicos reales — todo aviso, el único bloqueo sigue siendo el índice de ADRs | aceptado |
| [0039](0039-comando-fuente-forzada-diagrama-autoria-knob-por-lab.md) | El comando es la fuente forzada, el diagrama-primero es hábito de autoría, y la dirección del acoplamiento es un knob por-lab (reafirma 0036): el `.md` es fuente porque es el ejecutable (`isExecutable=false` en el bpmn) y no hay generador `bpmn→md`; se separa autoría (dibujar primero, sano) de enforcement (comando-primero); un lab model-driven puede voltear su ley, pero solo con un generador o validador de conformidad — re-etiquetar sin mecanismo es muro falso | aceptado |
| [0040](0040-ritual-sin-punteros-arroba-bang-o-inline.md) | En el ritual todo es `@`, `!` o inline — nunca un puntero (extiende 0034 al ritual completo): los punteros de lectura se prohíben en los comandos ("entre puntero y nada, nada"); lo que vive en un artefacto con dientes se imprime con un `!` (gemelos `rutear`/`asientos`), no se copia en prosa; la exploración dirigida queda para cuando leer ES el trabajo del comando | aceptado |
| [0041](0041-gate-anti-pii-clase-publica-denylist-privada.md) | El gate anti-PII se parte en detector-de-formas público (el muro server-side: email real, ruta de usuario nombrada — clases, no instancias, porque un gate que contiene la PII que busca la re-publica) y denylist-de-cadenas privada (gitignoreada, cinturón pre-push); modelo de amenaza el accidente, no el adversario; nace de que una cuenta gh secundaria se coló en `infra.md`/`HANDOFF` de este repo público | aceptado |
| [0042](0042-gobierno-documental-por-estructura.md) | Gobierno documental por estructura (capa-2): el hermano estructural del sello — el motor se gobierna por hash, los documentos instancia-de-template (`brief`/`infra`/`CONTRIBUTING`) por **secciones** (ledger `docs-gobernados.json` + detector `estado-docs.ps1`); gobierno por estatuto (alterar la estructura = garantía nula); aviso por defecto, muro opt-in en CI (nunca en `verificar`), el grafo se queda con `auditar`; cierra el hueco del `CONTRIBUTING` sin template (KIT-2) | aceptado |
| [0043](0043-linterna-del-gobierno-vista-no-gate.md) | La linterna del gobierno (`estado-gobierno.ps1`): una **vista de solo lectura** que deriva el grafo del gobierno de la ley real (áreas, gates, documentos-dueño duras/blandas, capacidades, hooks, CI, y **huérfanos en rojo**) y lo emite a un `.html` autocontenido — para que el humano vea la máquina con sus ojos, no con la narración del agente (mata el "juez y parte"). No inventa verdad (copias byte-fieles del matcher/reglas), falla cerrado, y **es vista NO gate** (respeta ADR 0002; nace aviso por regla 2-3, como el atlas) | aceptado |
| [0044](0044-editor-del-gobierno-autora-gate-ejecuta.md) | El editor del gobierno: la extensión de VS Code **AUTORA** (clic derecho → ligar archivo↔capacidad con dirección y fuerza, escribe `tools/ligas.json`) y el gate determinista **EJECUTA** (`estado-ligas.ps1` en pre-push + CI **desde la base**, ADR 0003). El ledger es dato de instancia (NO se siembra; el evaluador sí); ligas rotas avisan y se excluyen; la UI nunca es el muro (ADR 0002 intacto: clic → JSON en git → gate, nada depende del modelo). Mata el aviso grueso "revisa las 89 capacidades" → nombra la exacta | reemplazado **en la superficie** por [0048] (el principio "UI autora / gate ejecuta" sigue vivo) |
| [0045](0045-identidad-sistema-gobierno-configurable.md) | Jidoka es un sistema de gobierno configurable, no solo una metodología: el usuario parametriza el gobierno de su repo desde la UI (qué se vigila, qué se lee, qué régimen tiene cada pieza, qué no toca la IA) sin editar JSON a mano; la UI autora, el gate ejecuta (ADR 0002/0044 intactos). Nace la capacidad CFG-1; fase 1 cubre documentos + ritual + candados + regímenes, validada en 6 Gembas sobre una maqueta clickeable | aceptado |
| [0046](0046-contratos-y-regimenes.md) | Cada pieza tiene un régimen (`motor` sellado por hash / `estatuto` por estructura / `libre`); el régimen efectivo = fábrica + overrides firmados en `tools/contratos.json`, que es INSTANCIA no-clobber (jamás en la lista mecánica del manifiesto — el lazo la pisaría). No se duplica la verdad (hash vs secciones vs nada); el `estatuto` (antes nombrado por analogía con una marca ajena, retirada el 2026-07-21) se generaliza del documento al ritual; la clase `contrato` en la siembra (R3b) queda diferida con su ADR | aceptado |
| [0047](0047-meta-gobierno-contrasena-firma-candado.md) | El meta-gobierno son tres piezas: contraseña-ritual (deliberación pública en el README), firma (atribución derivada de `git config`, no inventada) y candado IA = hook `PreToolUse` que deniega en el momento nombrando el contrato — el único muro determinista; el `permissions.deny` es capa estática barata (matchea prefijo de comando, no ruta destino, por eso el hook es el enforcement). El disparo `deny-vs-ask` pasa de catálogo-solo a cableado | aceptado |
| [0048](0048-superficie-app-tuberia.md) | La superficie del gobierno es la **app de la tubería (Tauri v2)** cuya interfaz es la maqueta, literal (una ventana navegable: tubería, bandeja, formulario, modo avanzado) — supersede el ADR 0044 **en la superficie**: cambia el QUIÉN autora (la app, ya no la extensión), el principio "la UI autora, el gate ejecuta" (ADR 0002/0044) queda intacto. El motor PS es el único lector/escritor de los ledgers (la app lo invoca); la extensión se retira. Descarta web local, File System Access API (Chromium-only, no ejecuta procesos, duplicaría el motor) y Electron (~10x más pesado). `app/` Jidoka-only; fase 1 = Windows | aceptado |
| [0049](0049-pilar-de-flujo.md) | El pilar de flujo (JIT): el segundo pilar de la Casa TPS se construye como **contratos deterministas con gate**, no como disciplina — contrato del HANDOFF/ROADMAP/CHANGELOG con límites como dato de instancia en `tools/flujo.json` + checks en el motor (el check `[contrato-handoff]` **BLOQUEA** el HANDOFF que crece; lo archivado vive en `docs/handoff-historico.md`, que `/arranca` nunca inyecta), expiración automática por script (muerte por defecto), límite WIP que planta a `/planea` con Gemba vencido, vista `estado-flujo -Json` + hook SessionStart, reporte sin jerga, y el reparto autoridad-del-dominio vs dueño-operador. Descarta el asiento-capataz (en Toyota no existe), Scrum (points/velocity/standups, descartado con fuentes) y las reglas-como-prosa (lo que falló: el HANDOFF pedía por escrito limpiarse y llegó a 419 líneas). 9 rebanadas M1→M2→M3; este ADR registra el movimiento 1 (R1) | aceptado |
| [0050](0050-molde-unico-de-los-adrs.md) | El molde único de los ADRs: un **guardián residente que bloquea**, no una skill personal. El corpus se alinea a una sola estructura (5 secciones canónicas + estado coherente header↔índice) y `tools/probar-adrs.ps1` (sembrado, mecánica) lo hace cumplir con `exit 1` en CI + preflight — segundo muro real del repo. Causa raíz: la skill personal `adr-helper` que creaba ADRs vivía fuera del repo y ya había divergido (`Razones` vs `Por qué`). Descarta solo-avisar (el cliente eligió bloquear), la skill personal como garantía (no viaja) y el enforcement en `verificar.ps1` (se queda como `probar-*` en CI, principio de ADR 0042) | aceptado |
| [0051](0051-tuberia-mapa-completo-por-convencion.md) | La tubería deja de leer una lista curada a mano y se vuelve el **mapa completo del repo, derivado por escaneo** (`git ls-files` + tabla de convención `$TIPOS` + catch-all por carpeta, todo en el lector `tuberia-datos.ps1`): cada archivo aparece por existir (nada invisible), soltar uno lo muestra solo, retirar una carpeta lo cura solo; el nombre se deriva del propio archivo (`# H1` / `/jidoka:<x>`). Extiende CFG-1 al censo. Descarta la lista curada (es el drift que combate), el frontmatter por archivo (~40 `.ps1` sin cabecera) y un JSON de config (pedido del cliente: la regla en el código). Aristas vacías a propósito (se cablean desde la app, sprint futuro) | aceptado |
