# Cuadro de cierre — 2026-07-23 (exploración: procedencia del backlog + enredo de asientos)

> Segundo cierre del día (el primero fue la huella en labs, `v1.32.0`). Hechos medibles, no impresiones. Un hueco se declara, no se rellena.

| Hecho | Valor |
|---|---|
| Sprint · ¿terminó o en curso? | **Sin sprint** — sesión de exploración (tres vueltas de la kata). Terminada. |
| Rebanadas: planeadas / entregadas / desviadas | N/A (no hay plan-contrato; la exploración no rebana). |
| Rama · commits | `exploracion/procedencia-del-backlog-20260723` · 2 commits al momento del cuadro (`3e2ea23` procedencia+casting+allowlist+curas, `f9db012` mapa de asientos) + 1 pendiente de commit (guiones del casting + HANDOFF + CHANGELOG + este cuadro). |
| Working tree al cerrar | Cambios sin commitear pendientes de este mismo cierre; se commitean antes del merge. |
| PR · ¿rama eliminada? | **#130**, OPEN. Merge y borrado de rama: pendiente de la orden final del cliente (condicionada a auditores limpios). |
| Ritual corrido | `arranca`, `cierra`. (Sin `planea`/`gemba`: es exploración.) |
| Delegaciones | 4 `auditor` (actas de los transcripts de ayer/hoy) · 4 `auditor` (auditoría de la sesión propia) · 3 subagentes al enredo de asientos (`explorador` localización, `auditor` coherencia, `claude-code-guide` mecanismo CLI) · 4 `auditor` de cierre (esta fase). El hilo principal escribió ROADMAP/MUERTOS/análisis/HANDOFF/CHANGELOG **sin marcador 🎭** — hueco de forma anotado (esas rutas no las gobierna un rol del router). |
| Aprobaciones nombradas del cliente (parafraseadas — no van citas textuales al repo público, ADR 0055) | Aprobó el candado pero como backlog, sin construir (2026-07-23) · autorizó commitear con libertad · confirmó el nombre del asiento de QA · eligió la opción A (casting nominal fuera de git) · marcó importantísimo revisar el enredo de los agentes en sesión aparte · autorizó merge+release **condicionado a que los auditores de cierre salgan limpios**. |
| Pruebas automáticas: altas/cambios/bajas | 0 / 0 / 0 — no se tocó código de motor ni suites. |
| Pruebas E2E | N/A. El único experimento (allowlist del hook) corrió como lab desechable headless (`claude -p`), no como suite; evidencia en el informe, lab borrado. |
| Evidencia en `qa_runs/` | No aplica — exploración documental; la evidencia son los informes de `docs/analisis/` (rastreados por git) y los guiones de revisión. |
| Archivos: creados / editados | Creados: 4 informes (`exploracion-procedencia`, `-allowlist-por-asiento`, `-casting-vs-mecanica`, `-modelo-de-asientos`) + este cuadro. Editados: `ROADMAP.md`, `docs/MUERTOS.md`, `docs/analisis/README.md`, `HANDOFF.md`, `CHANGELOG.md`, `PRODUCT_BRIEF.md`, `handoff-historico.md`, `exploracion-allowlist` (cierre de un «no medido»). |
| Gates: verificar / probar-flujo / estado-docs / probar-version | **Verdes los cuatro** (exit 0). `[contrato-roadmap]` 41 ítems, 67/90 líneas. `probar-version` en rojo al primer cierre (SSOT en 1.32.0 vs CHANGELOG 1.33.0 — lo cazó un auditor de cierre); alineado `version.txt`/`package.json`/badge a `1.33.0` y vuelto verde antes del push. |
| ¿Compactación? · ¿re-verificado? | No hubo compactación en esta sesión. |
| ADRs creados / enmendados | Ninguno — es exploración; la doctrina exige ADR al construir, no al explorar. La regla de procedencia y el candado piden ADR **cuando se cableen** (anotado en sus tarjetas). |
| CHANGELOG · versión | Al día. **`v1.33.0` (MINOR)** — solo `docs`; en un repo de método, una regla de gobierno + reinicio del backlog es contenido, no ruido. |
| Motor Jidoka al día | Sí — no se tocó motor. |
| Issues/hallazgos | Sin issues de GitHub. Hallazgos → 16 tarjetas al ROADMAP, todas con puntero y punto de Gemba. Colateral: `benchmark-panorama-202607.md` de ayer nunca se commiteó y se perdió (registrado). |
| Fricción (Kaizen crudo) | Correcciones del cliente: ~4 (pidió delegar con subagentes dos veces, frenó una sobreproducción de agentes, pidió una explicación más llana, exigió respetar el modelo de la kata). Errores del agente reparados: me salté la kata hasta que el cliente lo señaló; una cifra de supervivientes del backlog que ningún artefacto respaldaba; no apliqué la regla de guion a mis propias tarjetas; presenté como «ley de plataforma» dos cosas que eran decisión del repo (una skill sin modelo propio, el renombre de un agente); y en este mismo cierre se me coló una frase textual del cliente entre comillas al ROADMAP/HANDOFF que un auditor de confidencialidad atrapó. **Los auditores de cierre (4, ciegos) cazaron además:** el SSOT de versión sin alinear (`probar-version` en rojo — el required check del CI habría bloqueado el merge); una cifra intermedia falsa que sobrevivió a su propia corrección (dije «36», el real era 39; ya no se cita ningún número de tránsito); una contradicción «mata 41 / poda 32»; el desfase de versión en `docs/analisis/README.md`; y un frontmatter faltante. Todos corregidos antes del push. |
| Pendientes al HANDOFF | La cola de decisiones del cliente (5 puntos, ver HANDOFF), encabezada por el merge+release condicionado y la sesión a fondo del modelo de asientos. |
| Resumen de cambios (3-5 líneas) | Backlog reiniciado 62→41 bajo regla de procedencia; 32 huérfanos a MUERTOS con texto íntegro, 9 rescatados con puntero verificado. 4 informes de exploración nuevos + mapa del enredo de asientos. Casting decidido fuera de git. Nada de mecanismo construido: todo a tarjeta con puntero y guion. |
| Resumen de la conversación | El cliente pidió leer los chats de ayer/hoy («cómo andamos pensando»), un asiento de exploración que solo escriba en ROADMAP/análisis, y limpiar el ROADMAP. Derivó en el reinicio del backlog con regla de procedencia, la medición del hook (viable), el descubrimiento del enredo de asientos (marcado importantísimo para sesión aparte), y la decisión de mantener el casting nominal fuera de git. |
