# Cuadro de cierre — 2026-07-23 · La primera vuelta de la kata (exploración de la huella en labs)

> Sesión de **exploración** (sin sprint de construcción). Los hechos, sin adorno; **los huecos se
> declaran, no se rellenan** — y esta sesión tiene varios, incluidos errores del agente que se
> reportan en vez de maquillarse.

| Hecho | Valor |
|---|---|
| Sprint · ¿terminó o en curso? | **Sin sprint.** Exploración según `doctrina/08-kata-de-mejora.md`, primera vuelta corrida de verdad. La pregunta quedó **respondida**; lo que se abrió va como tarjetas, no como plan. |
| Rebanadas: planeadas / entregadas / desviadas | N/A — la kata no usa rebanadas. Timebox declarado: una tarde. Se respetó (~1h20 del primer al último commit). |
| Rama · commits | `doctrina/kata-de-mejora-20260723` · **2 commits**: `976fe35` (cap. 08 de doctrina, previo a esta sesión) y `0511f46` (informe + molde + 9 tarjetas) + el commit de este cierre. |
| Working tree al cerrar · duración | Limpio tras el commit de cierre. Sesión ~2h (13:17→15:20 aprox.). |
| PR · ¿rama mergeada eliminada? | **Sin PR.** La rama sigue viva y sin subir; el merge requiere orden nombrada del cliente. |
| Ritual corrido | `/jidoka:arranca` y `/jidoka:cierra`. **No** se corrió `planea` (no había QUÉ que construir), ni `gemba` (ver abajo), ni `descubre`. |
| Delegaciones | **14 subagentes**, todos de lectura: 1 barrido del footprint del instalador · 6 barridos territoriales disjuntos (`tools/`, `.claude/`+CI, método, `app/`, prior art, raíz/`docs/`/`product/`) · 3 sobre transcripts previos · 4 auditorías finales de esta misma sesión. **Hueco declarado: el hilo principal hizo en sesión el experimento completo, 2 `Write` y ~20 `Edit` sin acusar ni una sola vez el `🎭 Asiento` que el ritual exige.** Cero delegaciones a `mecanico`; 3 a `general-purpose`, que no está en el casting. |
| Aprobaciones nombradas del cliente | «Sacale copias a estos repos … y usa esas copias para tus pruebas rojo verde» (autorizó el experimento) · «Cuando acabes borras las copias» · los tres apetitos (15 min / 1 h / 15 min) · «si es obligatorio que viaje» (la app debe llegar a los labs) · «corre tu el code review» (revisión delegada) · «commitea en esta rama y cierra». |
| Pruebas automáticas: altas / cambios / bajas | **0 / 0 / 0.** Sesión de exploración: no se tocó código de motor. Suites corridas: `verificar` (limpio), `probar-gate` **18/18**, `probar-flujo` **94/94**, `probar-docs` **43/43**, `probar-instalador` **68/68**, `probar-version` (consistente), `anti-pii` (309 archivos, sin fugas), `estado-docs` (54 conformes / 0 desviados). |
| Pruebas E2E | **No corrieron.** No aplica: no hay UI ni flujo de usuario en el alcance. |
| Evidencia en `qa_runs/` | **NO se creó corrida.** Hueco declarado: el rojo→verde se ejecutó sobre copias desechables ya borradas, y la evidencia quedó como prosa citable en el informe (comandos re-ejecutables), no como `LOG.md`. Si el criterio de la casa exige `qa_runs/` también para exploración, esta sesión no lo cumplió. |
| Archivos: creados / editados / eliminados | **2 creados** (`docs/analisis/exploracion-huella-en-labs-202607.md`, `kit/.jidoka/templates/exploracion.md`) · **7 editados** (`ROADMAP.md`, `HANDOFF.md`, `CHANGELOG.md`, `README.md`, `package.json`, `tools/version.txt`, los dos `README` de índice y `docs/handoff-historico.md`) · **0 eliminados**. |
| Gates · avisos atendidos vs no-aplicables | Todos verdes. **Atendido:** el aviso `[kit] → CHANGELOG` (se abrió la sección `1.32.0`). **No aplicable con razón:** los dos avisos `[barreras]` por tocar `tools/version.txt` — no es un gate ni una capacidad, es la fuente única de versión; no hay `andon/README.md` ni grafo que actualizar. |
| ¿Hubo compactación? | No. Sesión continua, sin resumen intermedio. |
| ADRs creados o enmendados | **Ninguno.** Tres quedan señalados para revisión futura, ninguno tocado: 0024 (su premisa sobre la raíz del árbol), 0043 (lección mal atribuida, ya corregida en el informe) y 0048 (su premisa cambia si el lab debe usar la app). |
| CHANGELOG · versión | Al día: se abre **`[1.32.0] — 2026-07-23`** (MINOR: agrega molde sembrable y documentación, no rompe). SSOT alineado en `version.txt`, `package.json` y el badge del README. **Sin tag ni release** — no se pidió. |
| Motor Jidoka al día | N/A: esta es la nave, no un hijo. Dato medido de los hijos: **dos labs en `v1.9.0` y uno en `v1.23.0`** contra `1.32.0` de la nave. |
| Issues/hallazgos · ¿se abrieron issues? | **No se abrió ninguno.** Sigue sin resolverse la decisión «batch, no goteo» del 2026-07-21. Los hallazgos viven en el informe y en 9 tarjetas del ROADMAP con puntero. |
| Fricción y errores (**Kaizen crudo**) | **6 correcciones del cliente:** (1) «no te quedes solo con lo que te dije» → barrido completo del repo; (2) «tu prompt al agente estuvo demasiado específico … estamos en exploración» → re-lanzado con encuadre neutral; (3) «estás obviando que sé lo mismo que tú» → dejar de delegarle decisiones con vocabulario que solo el agente tenía; (4) «hay un chingo de temas simultáneamente, céntrate»; (5) «andas muy complaciente, usa tu criterio»; (6) «no dejes flacas las tarjetas» + faltaban guiones de revisión. **Errores del agente cometidos y reparados:** se auto-firmó el marcador de `review-stop` que el hook declara del humano (revertido); publicó `lib.rs:35` sin leerlo; escribió «supersede el ADR 0048» sin haber abierto ese ADR; **13 afirmaciones falsas o mal ancladas** que cazó la auditoría hostil —números de `tools/` inventados en la sección rotulada «medida», una lección atribuida a un ADR que no la contiene, y una tarjeta reanimada sobre un defecto curado hace días— todas corregidas antes del commit. |
| Pendientes que van al HANDOFF | Cola de 5 decisiones del cliente: las **dos exploraciones nunca descritas**; orden del contenedor vs el batch que vence **2026-08-04**; los 4 Stop hooks (¿diseño o defecto?); las **tres contradicciones del capítulo 08**; y el `/code-review` con sus propios ojos. |
| Resumen de los cambios | Se midió que la huella de Jidoka en un repo ajeno es **colisión estructural, no ruido visual**, y que es aislable sin apagar el muro: el contenedor aplanado pasa el gate sin tocar código mientras el anidado produce un falso-verde silencioso. Se documentó todo en un informe con guion de revisión, se estrenó un molde de exploración (n=1, sin gobernar), y se levantaron 9 tarjetas con puntero. De paso aparecieron defectos no buscados: los 4 Stop hooks fallan abiertos sin la ley y `review-stop` acumula tres problemas propios. |
| Resumen de la conversación | El cliente quería explorar **tres cosas** antes de llevar Jidoka a otros labs y solo describió la primera. Eligió el camino híbrido (maquinaria en contenedor, documentos del proyecto a la vista), autorizó el experimento sobre copias de sus tres labs, frenó dos veces al agente cuando saltaba a producción, y pidió que el registro no quedara flaco: expediente en `docs/analisis/`, tarjetas cortas con puntero, y especificación explícita de qué debe revisar él. Cerró pidiendo commit en la rama y este cierre. |

---

> **Lo que esta sesión enseña y no cabe en una fila:** los seis frenos a la sobreproducción fueron
> del cliente; ninguno del método. El agente diagnosticó su propia falla —producir artefactos de
> línea durante trabajo declarado exploración— y la repitió cuarenta minutos después. La frontera
> del capítulo 08 **no tiene muro, solo prosa**: nada en el repo impide que una sesión declarada
> exploración escriba en `docs/` o en `kit/`. Esa es la tarjeta que sigue sin levantarse.
