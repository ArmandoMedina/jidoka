{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidades por defecto de la ventana principal. R2: solo el core. R3+ suma los permisos de shell/dialog que el motor necesite.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
